# rsaswitch
Bash script to switch between RSA

Tested only on OS X

# How to use
- Copy `rsaswitch` into your bin folder
- `chmod +x rsaswitch`
- Name your rsa files as id_rsa_<PROFILE> and id_rsa_<PROFILE>.pub
- Run `rsaswitch <PROFILE>` to switch between profiles
